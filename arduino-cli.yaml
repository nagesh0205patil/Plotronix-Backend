# Arduino CLI Configuration for PlotTronix VI Lab
# Arduino Nano (ATmega328P, 5V, 16MHz)

board: arduino:avr:nano
build:
  default:
    flags: -v
properties:
  upload.maximum_size: 28672
  upload.maximum_data_size: 2048
  cpu: atmega328p
